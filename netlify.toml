[build]
  command = "mkdir -p dist && cp index.html app.js data.json dist/"
  publish = "dist"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/publish"
  to = "/.netlify/functions/publish"
  status = 200
